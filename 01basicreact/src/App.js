import Chai from './Chai'
function App() {
  return (
   <div>
   
    <h1>First React App | Atalique
      learned how to create react app via npx create-react-app 
      deleted all the unnecessary files build the app and start the app via npm start run 
      we have also run our file via yarn create-react app and yarn start to start the app.
    </h1>
    <Chai/>
   </div>
  );
}

export default App;
